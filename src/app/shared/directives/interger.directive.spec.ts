import { IntergerDirective } from './interger.directive';

describe('IntergerDirective', () => {
  it('should create an instance', () => {
    const directive = new IntergerDirective();
    expect(directive).toBeTruthy();
  });
});
